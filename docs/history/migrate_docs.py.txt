from pathlib import Path
import re

p = Path('.')
master = (p/'UMAN_EVENT_MANAGER_SPEC_v2.4.md').read_text(encoding='utf-8')
master = master.replace('v2.4', 'v2.6').replace('**Version:** 2.4', '**Version:** 2.6')
master = master.replace('offline-first', 'cloud-first').replace('Offline-First', 'Cloud-First')
master = master.replace('MVP is fully offline, single-manager, single-device. No cloud, no sync, no RBAC. Future architecture anticipates multi-user sync and permissions without implementing them.', 'Yonatan and Yosef use separate authenticated administrator accounts with event membership. Supabase PostgreSQL is canonical; committed changes propagate automatically through realtime. Local data is a non-authoritative read cache.')
master = master.replace('A single operations manager', 'Two authenticated operations administrators')
master = master.replace('100% local operation', 'Server writes; offline cached reads').replace('Local DB backups', 'Operator-managed server backup/recovery').replace('Local machine security', 'Auth, RLS, TLS and secure local storage').replace('Action logging (local)', 'Mandatory server-generated attributed audit').replace('Optimized for single device', 'Responsive on both administrator devices')
master = master.replace('| Future Sync | Future — Constraints Defined | Architecture supports future sync |', '| Realtime Sync | Mandatory | Automatic propagation and reconciliation |').replace('| Future Permissions | Future — Constraints Defined | RBAC concepts deferred |','| Event authorization | Mandatory | Membership RLS and administrator RPCs |')
master = re.sub(r'The system follows a strict isolation.*?distributed state\.', 'The MVP is cloud-first and multi-user. Phase 1 verifies an authenticated Event vertical slice before broader business features.', master, flags=re.S)
master = master.replace('Offline-first operation, encrypted local storage, secure backup/restore, audit readiness (structural)', 'Cloud-first server persistence, Auth, RLS, realtime, audit and read cache').replace('Cloud-first operation, encrypted local storage, secure backup/restore, audit readiness (structural)', 'Cloud-first server persistence, Auth, RLS, realtime, audit and read cache')
master = master.replace('Cloud backend, remote database, CRDT, Event Sourcing, distributed state', 'CRDT, Event Sourcing, peer-to-peer state').replace('Multi-user synchronization, sync UI, conflict-resolution UI', 'Character-level coediting and presence').replace('Single manager control (device owner)', 'Separate administrator accounts and event membership').replace('Remote authentication, RBAC UI, field-level permissions, invitations', 'Advanced role editor, field-level permissions UI, self-service invitations')
master = master.replace('There is no remote state, no editable shared state, and no user accounts for participants or staff.', 'External snapshots do not grant editable access. Administrators share authenticated server state; participant/staff accounts are out of scope.')
master = master.replace('during offline MVP operations', 'for event-scoped operations').replace('Read-only enforced at application layer', 'Read-only enforced by server mutation policy')
sections = {
25: ('Backup, Restore & Recovery', 'Canonical recovery uses operator-controlled PostgreSQL backups and isolated restore verification. No client may replace server data using a local cache. Preserve backup integrity, explicit destructive approval and recovery testing; encrypted portable export is deferred beyond the cloud foundation.'),
26: ('Security & Privacy', 'Supabase Auth is mandatory. TLS protects transport; event-membership RLS and restricted RPCs protect data. Sessions and small caches use platform-secure storage. No privileged key ships in Flutter. No password, token or passport data in logs. Passport access/retention must be reviewed before Person delivery; do not introduce custom encryption. See Technical v1.2 threat controls and ADR-001.'),
27: ('Cloud-First Architecture', 'PostgreSQL is the only source of truth. Flutter presentation uses application controllers and pure-Dart repository contracts implemented in Supabase infrastructure. Cached reads remain available temporarily offline, visibly stale. All writes require connectivity and server confirmation. No offline mutation queue or independent local master.'),
28: ('Realtime & Concurrency', 'Subscribe to domain changes, then reload canonical state on startup/login, subscription/reconnect, foreground, mutation and conflict. Every mutable record uses server attribution/timestamps and an integer version. Stale expected versions are rejected; preserve the draft and require intentional resolution. No silent last-write-wins. Realtime is notification, never integrity enforcement.'),
29: ('Authentication & Event Authorization', 'Yonatan and Yosef have distinct Supabase Auth accounts. event_members assigns administrator roles per event. RLS rejects anonymous and non-member reads; RPCs enforce membership on every write. Provision memberships through controlled backend operations. Additional managers require membership, not schema redesign.'),
30: ('Mandatory Audit Trail', 'Every material mutation atomically emits a server-generated audit entry: id, event_id, actor_user_id, timestamp, entity_type, entity_id, operation, old/new snapshots. Client writes to audit are forbidden. Archive/restore and later payment, assignment and alert operations must be attributable. Privileged backend administration remains a trusted operational boundary.'),
32: ('Database Migration & Versioning', 'Version-controlled Supabase SQL migrations define schema, indexes, FKs, checks, RLS, grants, functions, triggers and publication. Deploy in order through backend tooling, never from a phone. Destructive changes require verified backups. PostgreSQL transactions roll back failed migration steps. Clients use compatible API versions and rebuild disposable caches.'),
}
for n,(title,body) in sections.items():
    master = re.sub(rf'## SECTION {n}:.*?(?=## SECTION {n+1}:)', f'## SECTION {n}: {title}\n\n{body}\n\n', master, flags=re.S)
master = re.sub(r'> All persistent entities include standard infrastructure metadata columns:.*?Phase 1\.', '> All mutable business entities require event scope, server UUIDs, timestamps, created_by, updated_by, version and soft-delete metadata. Junctions carry event_id with composite same-event FKs. Audit is immutable; deterministic alert IDs are retained. Technical v1.2 contains the entity migration review.',master)
master = master.replace('last_modified_by_device_id (TEXT, nullable)', 'created_by (UUID, required), updated_by (UUID, required)').replace('actor_id (UUID, nullable, future)', 'actor_user_id (UUID, required)').replace('MVP: structural only, critical operations logged', 'Mandatory server-generated entries for all material mutations')
master = re.sub(r'### Poor/No Connectivity.*?(?=## SECTION 36:)', '### Poor/No Connectivity\nShow timestamped cached state, disable mutations and reconcile automatically on reconnect.\n\n### Multi-Manager Conflicting Edits\nReject stale versions, preserve entered text, reload canonical state and require an intentional retry.\n\n', master, flags=re.S)
master = re.sub(r'### AC-08:.*?(?=### AC-09:)', '### AC-08: Offline resilience and realtime\n1. Two distinct member accounts open the same event.\n2. One commits a change; the other sees it without refresh.\n3. Disconnect one device: cached data stays visible with offline warning; writes disabled.\n4. Reconnect: missed changes reconcile automatically.\n5. Submit stale version: reject, retain draft and show current record.\n6. Anonymous/outsider reads return no event data.\n\n', master, flags=re.S)
master = master.replace('No network dependency for any MVP feature','Cached reads only offline; server-confirmed online writes')
master = master.replace('2. Deploy app version expecting schema N+1','2. Deploy server migration N+1').replace('3. App detects version mismatch, creates pre-migration backup','3. Operator verifies pre-migration backup').replace('4. Migration executes, transforms schema','4. Server migration executes transactionally, transforms schema')
master = master.replace('1. Source: SQLite integrity check fails','1. Source: Server integrity check fails').replace('Single entity CRUD | - | - | < 50ms','Single entity CRUD | - | - | Measure server latency separately; never imply local commit')
master = re.sub(r'Technical choices formerly recorded.*?(?=\n\n)', 'Technical architecture is defined by v1.2 and ADR-001: Supabase/PostgreSQL, Auth, RLS, realtime, CAS versions, server audit and secure disposable cache.',master,flags=re.S)
master = re.sub(r'^\| DEF-00[123] .*$', '| Cloud foundation | Mandatory Phase 1 | Auth, RLS and realtime are no longer deferred |',master,flags=re.M)
master = re.sub(r'### Offline Audit.*?(?=### |\Z)', '### Cloud architecture audit\nHistorical offline audit superseded. Phase 1 status and evidence are tracked in PHASE1_PROGRESS.md; cloud and device tests are not presumed passed.\n\n',master,flags=re.S)
master = master.replace('**AUDIT RESULT: IMPLEMENTATION BASELINE — ready for Phase 1 subject to the explicit Product Owner deferrals.**','**AUDIT RESULT: Product requirements retained; cloud implementation verification is tracked separately in PHASE1_PROGRESS.md.**')
header = '\n> Architecture migration 2026-09-17: ADR-001 and Technical v1.2 supersede previous persistence assumptions. Phase 1 is Authenticated Cloud Persistence & Realtime Foundation (Event slice). Broader MVP business requirements below remain required in subsequent phases, not claims of implemented features. The referenced Breslov design markdown is absent; preserve available reference assets and obtain it before visual sign-off.\n'
master = master.replace('\n',header,1)
(p/'UMAN_EVENT_MANAGER_SPEC_v2.6.md').write_text(master,encoding='utf-8')
for name in ['UMAN_EVENT_MANAGER_SPEC_v2.4.md','UMAN_EVENT_MANAGER_SPEC_v2.5.md','UMAN_EVENT_MANAGER_TECH_SPEC_v1.1.md']:
    f=p/name
    archive=p/'docs/history'/name
    archive.parent.mkdir(parents=True,exist_ok=True)
    archive.write_text('> HISTORICAL ONLY — superseded by Master v2.6 / Technical v1.2 / ADR-001. Not implementation instructions.\n\n'+f.read_text(encoding='utf-8'),encoding='utf-8')
    f.write_text('# Superseded specification\n\nUse [Master v2.6](UMAN_EVENT_MANAGER_SPEC_v2.6.md), [Technical v1.2](UMAN_EVENT_MANAGER_TECH_SPEC_v1.2.md) and [ADR-001](ADR-001-CLOUD-FIRST-REALTIME-MULTIUSER.md).\n\nHistorical text: [archive](docs/history/'+name+').\n',encoding='utf-8')
for f in (p/'spec_parts').glob('*.md'):
    nums=[int(n) for n in re.findall(r'^## SECTION (\d+):',f.read_text(encoding='utf-8'),re.M)]
    chunks=[]
    for n in nums:
        m=re.search(rf'^## SECTION {n}:.*?(?=^## SECTION \d+:|\Z)',master,re.M|re.S)
        if m: chunks.append(m.group())
    f.write_text('> Sectional copy of Master v2.6. ADR-001 and Technical v1.2 govern cloud architecture.\n\n'+''.join(chunks),encoding='utf-8')
